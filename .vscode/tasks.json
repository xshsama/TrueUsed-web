{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "web: build check",
			"type": "shell",
			"command": "npm run -s build",
			"problemMatcher": [
				"$eslint-stylish"
			],
			"group": "build"
		},
		{
			"label": "web: build check",
			"type": "shell",
			"command": "npm run -s build"
		}
	]
}